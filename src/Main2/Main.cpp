#include "../Base/Base.h"

int main()
{
    // auto mat = MatrixSimplePerspective(1.0f,10.0f);
    // Vector4 v = { 0,0,0.5f,1 };
    // v = mat * v;
    // Vector4Print(v);
    // v.x /= v.w;
    // v.y /= v.w;
    // v.z /= v.w;
    // v.w /= v.w;
    // Vector4Print(v);

    return 0;
}
void Main()
{
    auto boidsWorld = make_unique<BoidsWorld>();

    while(true)
    {
        auto deltaTime = GetDeltaTime();

        if (WindowWasClosed()) break;

        boidsWorld->Update(deltaTime);
        boidsWorld->Render();

        WindowRender(boidsWorld);
    }
}